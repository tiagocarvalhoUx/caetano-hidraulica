{
  "rewrites": [{ "source": "/(.*)", "destination": "/" }],
  "buildCommand": "npm run vercel-build",
  
  "framework": "vite",
  "outputDirectory": ".vite",

  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build"
    }
  ]
}
